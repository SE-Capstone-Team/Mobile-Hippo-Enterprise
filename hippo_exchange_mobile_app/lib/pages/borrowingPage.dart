import 'package:flutter/material.dart';

//PLACEHOLDER PAGE
class BorrowingPage extends StatelessWidget {
	const BorrowingPage({Key? key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Scaffold(
			appBar: AppBar(
				title: const Text('Borrowing Page'),
			),
			body: const Center(),
		);
	}
}